#include <stdio.h>

int main(){
    // 1. n개의 케이스 입력받기, 0이 나오면 끝이라고 암시.
    while(1){
        int input;
        int cnt;
        scanf("%d", &input);
        for(int n = input; n <= input*2; n++ ){
            if(n % )
        }
        if(input == 0){
            break;
        }
    }
    // 2. n 2n 사이의 소수 검색
    
    // 3. 각 갯수 출력
    return 0;
}